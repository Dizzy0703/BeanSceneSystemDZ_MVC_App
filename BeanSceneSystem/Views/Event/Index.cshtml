@model IEnumerable<BeanSceneSystem.Models.Event>

@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    <div>
        <h2 class="text-primary">List of All Events</h2>
    </div>
    <div class="col-6 align-items-end">
        @if (User.IsInRole("admin"))
        {
            <a asp-controller="Event" asp-action="Create" class="btn btn-primary" width="20%"><i class="bi bi-plus-circle-fill"></i>&nbsp;Add New Event</a>
        }
    </div>
</div>
<br/>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>            
            <th>
                @Html.DisplayNameFor(model => model.ReservationId)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>
                    @Html.DisplayFor(modelItem => item.Id)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.Description)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.Duration)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.UserId)
                </th>            
                <th>
                    @Html.DisplayFor(modelItem => item.ReservationId)
                </th>

                <td>
                    @if (User.IsInRole("admin"))
                    {
                        <a asp-controller="Reservation" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-primary mx-3">Edit</a>
                        <a asp-controller="Reservation" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-primary mx-3">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
